# 2026-07-17

## MusicHall 歌单功能 UI 实现
- 创建 `MusichallPlaylistItem`、`TagFilterGroup`、`TagFilterItem` 数据模型（common/musicbasic/data/）
- 在 MusicMemoryStore 种子 8 个演示歌单（含封面、播放量、标签）+ 3组分类标签
- MusicDbApi 新增 `getMusichallPlaylists()` 和 `getMusichallTagGroups()`
- 实现 `SourceTabBar` 组件：5 平台水平滚动 Tab + 右侧圆形筛选按钮
- 实现 `PlaylistCard` 组件：正方形封面 + 左下角播放量叠加 + 名称，点击 Push SongListPage
- 实现 `TagFilterSheet` 组件：BindSheet 内分组 Chip 标签筛选
- 重写 `MusicHallPage`：标题渐隐吸顶 + Grid 3列歌单网格 + BindSheet 标签筛选
- musicbasic Index.ets 和 musichall Index.ets 导出更新
- 修复编译错误：bindSheet 需 @Builder 包装而非直接传 @ComponentV2；Symbol/Color 资源名替换为有效值
- MusicHall UI 重构：标题对齐首页(.margin top 64)、SourceTabBar→ChipGroup 胶囊样式+沉浸光感图标(SymbolGlyph+BlurStyle)、TagFilterSheet→排序ChipGroup+SMALL标签chip、PlaylistCard→对齐MusicHome样式(play_round_triangle_fill/clip/Fill/fontSize14)
