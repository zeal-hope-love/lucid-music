/**
 * 音乐厅 — UI 占位，你需要重写
 */
@ComponentV2
export struct MusicHallPage {
  build() {
    Text('音乐厅 - 待重写')
      .fontSize(20)
      .width('100%')
      .height('100%')
      .textAlign(TextAlign.Center)
  }
}
